question - count ways to nth stair(order does not matter)
using recursion

long long countWays(int m)
    {
     if(m<=0)
     return 0;
     if(m==1)
     return 1;
     if(m==2)
     return 2;
     if(!(m%2))
     return countWays(m-1)+1;
     if(m%2==1)
     return countWays(m-1);
    }
